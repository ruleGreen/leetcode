class Solution:
    def addToArrayForm(self, A: List[int], K: int) -> List[int]:
        num = ''
        for i in range(len(A)):
            num = num + str(A[i])
        return [ int(i) for i in list(str(int(num)+K)) ]
        
